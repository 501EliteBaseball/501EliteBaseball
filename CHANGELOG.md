# Gemstone Button System — Phase A

## Added
- `gem-button-ruby.png`
- Ruby gemstone asset applied to primary red actions.

## Changed
- Primary buttons now use real PNG gemstone artwork instead of CSS-only gemstone simulation.
- Text remains live, accessible, centered, and responsive.
- Hover/tap effects move the whole gem without distorting the image.

## Note
Secondary buttons are intentionally left on the existing navy system until the sapphire asset is added.

## Upload
Upload everything inside this ZIP to GitHub root and overwrite.
Cloudflare deploy command remains: `bash deploy.sh`.
